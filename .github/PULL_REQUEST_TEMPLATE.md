<!-- 感谢贡献！请填写以下内容，便于审查。详细规范见 CONTRIBUTING.md -->

## 改动内容

<!-- 做了什么？为什么这么做？ -->

## 关联 Issue

Closes #

## 改动类型

- [ ] `feat` 新功能
- [ ] `fix` 缺陷修复
- [ ] `perf` 性能优化
- [ ] `refactor` 重构（不改变行为）
- [ ] `docs` 文档
- [ ] `test` 测试
- [ ] `chore` 杂项

## 测试

- [ ] `node --check video-hardsub-translator.user.js` 通过
- [ ] 6 个测试套件全部通过（306/306）
- [ ] 新增/修改的断言能**证明修复有效**（撤销修复会失败）

本地测试结果：

```
总计 ___ 项  ·  通过 ___  ·  失败 ___
```

## 行为影响

- [ ] 不改变既有行为（重构 / 性能优化）
- [ ] 改变了行为 —— 已在下方说明原因与迁移方式：

<!-- 若改变了行为，请说明：老用户已有配置是否仍然可用？ -->

## 性能（如适用）

- [ ] 已用 `node _test/bench-ab.mjs <旧> <新> 3` 对比，数据如下：

```
粘贴 A/B 基准输出
```

## 文档

- [ ] 已更新 `README.md`（如涉及配置项、使用方式、引擎）
- [ ] 已更新 `docs/`（如涉及架构、接口、测试）
- [ ] 已更新 `CHANGELOG.md`

## 自查

- [ ] 未引入第三方运行时依赖、构建步骤或浏览器端模型下载
- [ ] 热路径未新增不必要的内存分配
- [ ] 所有 `innerHTML` 走 `setHTML()`，用户数据经 `escapeHtml()`
- [ ] 新增配置项已同步 `DEFAULTS` 与 `sanitizeCfg()`
- [ ] 我的贡献以 GPL-3.0 授权，且不包含第三方专有代码
